{
  "name": "windows11-enterprise-apo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx server.ts",
    "build": "vite build && esbuild server.ts --bundle --platform=node --format=cjs --packages=external --sourcemap --outfile=dist/server.cjs",
    "start": "node dist/server.cjs",
    "preview": "vite preview",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@google/genai": "^2.4.0",
    "@tailwindcss/vite": "^4.1.14",
    "@vitejs/plugin-react": "^5.0.4",
    "dotenv": "^17.2.3",
    "express": "^4.21.2",
    "lucide-react": "^0.546.0",
    "motion": "^12.23.24",
    "react": "^19.0.1",
    "react-dom": "^19.0.1",
    "vite": "^6.2.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.14.0",
    "autoprefixer": "^10.4.21",
    "esbuild": "^0.25.0",
    "tailwindcss": "^4.1.14",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2"
  }
}import tailwindcss from '@tailwindcss/vite';
import react from '@vitejs/plugin-react';
import path from 'path';
import { defineConfig } from 'vite';

export default defineConfig(() => {
  return {
    plugins: [react(), tailwindcss()],
    resolve: {
      alias: {
        '@': path.resolve(__dirname, '.'),
      },
    },
    server: {
      port: 3000,
    },
  };
});{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src", "server.ts"]
}node_modules
dist
.env
.DS_Store
*.logimport express from "express";
import path from "path";
import { createServer as createViteServer } from "vite";
import { GoogleGenAI } from "@google/genai";
import dotenv from "dotenv";

dotenv.config();

async function startServer() {
  const app = express();
  const PORT = 3000;

  app.use(express.json());

  // Avcı AI yerel akıllı yanıt motoru
  const getSmartFallbackReply = (message: string): string => {
    const q = message.toLowerCase();
    if (q.includes("memz") || q.includes("virüs") || q.includes("trojan")) {
      return "⚠️ DİKKAT apo! Masaüstündeki MEMZ_Payload.exe simülasyonu çalıştırıldığında Windows 11 glitch efektleri, Nyan Cat animasyonu ve BSOD tetiklenir. Güvenlik modülüyle dilediğin zaman sistemi kurtarabilirsin!";
    }
    if (q.includes("undertale") || q.includes("sans")) {
      return "💀 Sans savaşı için apo'ya özel taktik: Mavi kemiklerde kesinlikle dur, blaster ateşlendiğinde köşelere kaç! Kararlılığını kaybetme apo.";
    }
    if (q.includes("apo") || q.includes("kimsin")) {
      return "👑 apo, bu sistemin (apo-PC) mutlak Yöneticisidir (Administrator). Ben de apo'nun komutlarını yerine getiren Windows 11 yerel yapay zeka asistanı Avcı AI'yım.";
    }
    return `apo, "${message}" komutunu aldım. Windows 11 Enterprise çekirdeğim arka planda sistemi stabil tutuyor.`;
  };

  app.post("/api/avci-chat", async (req, res) => {
    const { message } = req.body;
    if (!message) return res.status(400).json({ error: "Mesaj giriniz" });

    const apiKey = process.env.GEMINI_API_KEY;
    if (!apiKey) {
      return res.json({ reply: getSmartFallbackReply(message), mode: "local" });
    }

    try {
      const ai = new GoogleGenAI({ apiKey });
      const promptText = `Sen Windows 11 Enterprise (apo) işletim sisteminin yerel asistanı "Avcı AI"sın. Yönetici apo'ya sadık, zeki bir Windows dostusun. Mesaj: "${message}"`;
      const response = await ai.models.generateContent({
        model: "gemini-3.8-flash",
        contents: [{ role: "user", parts: [{ text: promptText }] }]
      });
      return res.json({ reply: response.text, mode: "gemini" });
    } catch {
      return res.json({ reply: getSmartFallbackReply(message), mode: "fallback" });
    }
  });

  app.get("/api/health", (req, res) => {
    res.json({ status: "ok", os: "Windows 11 Enterprise (apo)", user: "apo" });
  });

  if (process.env.NODE_ENV !== "production") {
    const vite = await createViteServer({
      server: { middlewareMode: true },
      appType: "spa",
    });
    app.use(vite.middlewares);
  } else {
    const distPath = path.join(process.cwd(), "dist");
    app.use(express.static(distPath));
    app.get("*", (req, res) => {
      res.sendFile(path.join(distPath, "index.html"));
    });
  }

  app.listen(PORT, "0.0.0.0", () => {
    console.log(`Server running on http://localhost:${PORT}`);
  });
}

startServer();export type AppId =
  | 'user'
  | 'edge'
  | 'undertale'
  | 'avci'
  | 'memz'
  | 'notepad'
  | 'paint'
  | 'cmd'
  | 'taskmgr'
  | 'settings'
  | 'calculator';

export interface WindowState {
  id: AppId;
  title: string;
  icon: string;
  x: number;
  y: number;
  width: number;
  height: number;
  isMinimized: boolean;
  isMaximized: boolean;
  zIndex: number;
}

export type MemzStage =
  | 'idle'
  | 'warning1'
  | 'warning2'
  | 'payload'
  | 'bsod'
  | 'nyan'
  | 'recovery'
  | 'recovering';

export interface PopupAlert {
  id: string;
  title: string;
  text: string;
  x: number;
  y: number;
  icon: 'error' | 'warning' | 'info';
}

export interface ChatMessage {
  id: string;
  sender: 'user' | 'avci';
  text: string;
  timestamp: string;
}

export interface DesktopFile {
  id: string;
  name: string;
  extension: string;
  size: string;
  date: string;
  type: 'text' | 'image' | 'game' | 'code' | 'folder';
  content?: string;
  imageUrl?: string;
}├── package.json
├── vite.config.ts
├── tsconfig.json
├── server.ts
├── index.html
├── .gitignore
└── src/
    ├── main.tsx
    ├── App.tsx
    ├── index.css
    ├── types.ts
    ├── utils/
    │   └── audio.ts                  (8-Bit Synthesizer & Undertale sesleri)
    └── components/
        ├── UndertaleApp.tsx          (Orijinal Sans, Papyrus, Undyne savaşları)
        ├── MemzSimulation.tsx        (MEMZ virüs simülasyonu, BSOD, Nyan Cat)
        ├── AvciAiApp.tsx             (apo'ya özel AI asistan)
        ├── Taskbar.tsx               (Windows 11 Görev Çubuğu & Başlat)
        ├── StartMenu.tsx             (Windows 11 Başlat Menüsü)
        ├── DesktopIcons.tsx          (Masaüstü simgeleri & kısayollar)
        ├── DesktopContextMenu.tsx    (Masaüstü sağ tık menüsü)
        ├── WindowFrame.tsx           (Aero/Mica pencereleri)
        ├── CalendarNotificationFlyout.tsx
        ├── QuickSettingsPanel.tsx    (Hızlı ayarlar: Wi-Fi, Ses, Parlaklık)
        ├── WidgetsBoard.tsx          (Hava durumu, Borsa, Haberler)
        ├── LockScreen.tsx            (apo Yönetici Kilit Ekranı)
        ├── EdgeApp.tsx               (Edge Tarayıcı simülasyonu)
        ├── NotepadApp.tsx            (Not Defteri)
        ├── PaintApp.tsx              (Çizim & Boyama uygulaması)
        ├── CalculatorApp.tsx         (Hesap Makinesi)
        ├── CmdApp.tsx                (Komut İstemi & Windows Terminal)
        ├── TaskManagerApp.tsx        (Görev Yöneticisi & Servisler)
        ├── SettingsApp.tsx           (Windows 11 Ayarları)
        └── UserFolderApp.tsx         (apo Dosya Gezgini)
